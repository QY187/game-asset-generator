# AI PR Reviewer - 功能点详细拆分（分阶段实现指南）

## 📋 总体规划

本项目将分为 **6个阶段**，每个阶段包含若干功能点，建议按顺序逐步实现。

---

## 🎯 阶段一：项目基础搭建（预计 1-2 天）

### 功能点 1.1：项目初始化
**目标：** 创建项目骨架，配置开发环境

**任务清单：**
- [ ] 创建项目目录结构
- [ ] 创建 `pyproject.toml` 配置文件
- [ ] 创建各模块的 `__init__.py` 文件
- [ ] 安装基础依赖包

**验证方式：**
- 项目结构完整
- 可以成功导入各模块

---

### 功能点 1.2：数据模型定义
**目标：** 定义核心数据结构，为后续开发奠定基础

**任务清单：**
- [ ] 定义 `PRReference` 模型（PR引用信息）
- [ ] 定义 `FileChange` 模型（文件变更）
- [ ] 定义 `PRMetadata` 模型（PR元数据）
- [ ] 定义 `PR` 模型（完整PR对象）
- [ ] 定义 `Risk` 模型（风险项）
- [ ] 定义 `Summary` 模型（变更摘要）
- [ ] 定义 `AnalysisResult` 模型（分析结果）
- [ ] 定义 `Config` 模型（配置）

**验证方式：**
- 所有模型可以正常实例化
- 模型字段验证正常工作

---

### 功能点 1.3：配置管理
**目标：** 实现配置的读取、存储和管理

**任务清单：**
- [ ] 定义配置文件路径（`~/.codesage/config.json`）
- [ ] 实现 `load_config()` 函数（加载配置）
- [ ] 实现 `save_config()` 函数（保存配置）
- [ ] 支持环境变量读取（`OPENAI_API_KEY`, `GITHUB_TOKEN`）

**验证方式：**
- 配置可以正常保存和读取
- 环境变量可以正确覆盖配置

---

## 🔗 阶段二：GitHub API集成（预计 2-3 天）

### 功能点 2.1：PR URL解析
**目标：** 从用户输入的URL中提取PR信息

**任务清单：**
- [ ] 支持标准GitHub PR URL格式
  - `https://github.com/owner/repo/pull/123`
  - `https://github.com/owner/repo/pull/123/files`
- [ ] 解析出：平台、owner、repo、pr_number
- [ ] 处理异常URL格式（错误提示）

**验证方式：**
- 输入有效URL，正确返回 `PRReference` 对象
- 输入无效URL，抛出明确错误

---

### 功能点 2.2：获取PR元数据
**目标：** 通过GitHub API获取PR的基本信息

**任务清单：**
- [ ] 射接GitHub API（`GET /repos/{owner}/{repo}/pulls/{pr_number}`）
- [ ] 提取：标题、描述、作者、状态、创建时间
- [ ] 提取：目标分支、源分支
- [ ] 提取：标签、负责人列表
- [ ] 处理API认证（使用Token提高限额）

**验证方式：**
- 输入PR编号，正确返回 `PRMetadata` 对象
- 未授权时仍可访问公开仓库

---

### 功能点 2.3：获取文件变更
**目标：** 获取PR中所有变更文件的详细信息

**任务清单：**
- [ ] 射接GitHub API（`GET /repos/{owner}/{repo}/pulls/{pr_number}/files`）
- [ ] 提取每个文件的：文件名、变更状态、增删行数
- [ ] 提取文件的 `patch` 内容（diff）
- [ ] 处理分页（大型PR可能有多页）

**验证方式：**
- 返回完整的 `FileChange` 列表
- diff内容正确提取

---

### 功能点 2.4：获取Commit列表
**目标：** 获取PR关联的所有Commit信息

**任务清单：**
- [ ] 射接GitHub API（`GET /repos/{owner}/{repo}/pulls/{pr_number}/commits`）
- [ ] 提取每个Commit的：SHA、消息、作者、日期

**验证方式：**
- 返回完整的 `Commit` 列表

---

### 功能点 2.5：PR完整获取
**目标：** 整合上述功能，一次性获取完整PR对象

**任务清单：**
- [ ] 实现 `fetch_pr(pr_url)` 方法
- [ ] 依次调用：解析URL → 获取元数据 → 获取文件 → 获取Commits
- [ ] 组装成完整的 `PR` 对象
- [ ] 计算统计信息（总文件数、总增删行数）

**验证方式：**
- 输入PR URL，返回完整 `PR` 对象
- 统计信息正确计算

---

### 功能点 2.6：发布PR评论
**目标：** 将分析结果发布到GitHub PR评论中

**任务清单：**
- [ ] 射接GitHub API（`POST /repos/{owner}/{repo}/issues/{pr_number}/comments`）
- [ ] 格式化评论内容（Markdown格式）
- [ ] 处理发布失败的情况

**验证方式：**
- 评论成功发布到指定PR
- 评论格式正确显示

---

## 🤖 阶段三：AI分析引擎（预计 3-5 天）

### 功能点 3.1：OpenAI API封装
**目标：** 射接OpenAI API，提供基础的AI调用能力

**任务清单：**
- [ ] 初始化OpenAI客户端
- [ ] 实现 `chat_completion()` 方法（同步调用）
- [ ] 实现 `async_chat_completion()` 方法（异步调用）
- [ ] 处理API错误和重试
- [ ] 追踪Token消耗

**验证方式：**
- 可以成功调用OpenAI API
- 返回正确的AI响应

---

### 功能点 3.2：提示词模板设计
**目标：** 设计高质量的提示词，确保AI输出准确

**任务清单：**
- [ ] 设计"变更摘要"提示词模板
  - 输入：PR标题、描述、diff内容
  - 输出：变更类型、核心变更点、风险提示
- [ ] 设计"风险识别"提示词模板
  - 输入：文件diff、文件路径、语言类型
  - 输出：风险列表（表格格式）
- [ ] 设计"修复建议"提示词模板
  - 输入：识别的风险列表
  - 输出：具体修复建议和代码示例

**验证方式：**
- 提示词可以引导AI输出预期格式
- 输出内容准确、可解析

---

### 功能点 3.3：变更摘要生成
**目标：** AI分析PR变更，生成简洁摘要

**任务清单：**
- [ ] 提取所有文件的diff内容
- [ ] 调用OpenAI生成摘要
- [ ] 解析AI响应，提取：
  - 变更类型分类
  - 核心变更点（最多3点）
  - 影响评估
- [ ] 组装成 `Summary` 对象

**验证方式：**
- 摘要准确反映PR变更意图
- 格式清晰易读

---

### 功能点 3.4：风险代码检测
**目标：** AI识别代码中的潜在风险

**任务清单：**
- [ ] 遍历每个变更文件
- [ ] 跳过非代码文件（.md, .json, .txt等）
- [ ] 调用OpenAI分析每个文件的风险
- [ ] 解析AI响应，提取风险列表：
  - 严重程度（Critical/Major/Minor）
  - 风险类型（安全/性能/稳定性/可维护性）
  - 文件位置
  - 问题描述
- [ ] 组装成 `Risk` 对象列表

**验证方式：**
- 能够识别常见风险（SQL注入、N+1查询等）
- 风险分类准确

---

### 功能点 3.5：修复建议生成
**目标：** 为每个风险提供具体的修复建议

**任务清单：**
- [ ] 调用OpenAI生成修复建议
- [ ] 解析AI响应，提取：
  - 问题详细解释
  - 修复建议
  - 代码示例
- [ ] 将建议附加到对应的 `Risk` 对象

**验证方式：**
- 建议具体可操作
- 代码示例正确

---

### 功能点 3.6：完整分析流程
**目标：** 整合上述功能，实现完整的分析流程

**任务清单：**
- [ ] 实现 `analyze(pr)` 方法
- [ ] 流程：生成摘要 → 检测风险 → 生成建议
- [ ] 记录分析耗时
- [ ] 记录使用的模型
- [ ] 组装成 `AnalysisResult` 对象

**验证方式：**
- 输入PR对象，返回完整分析结果
- 各部分信息完整

---

### 功能点 3.7：智能模型路由
**目标：** 根据任务复杂度自动选择合适的AI模型

**任务清单：**
- [ ] 定义模型选择规则：
  - 小型PR（<5文件）→ `gpt-4o-mini`
  - 中型PR（5-20文件）→ `gpt-4o`
  - 大型PR（>20文件）→ `gpt-4`
  - 安全敏感文件 → 强制使用 `gpt-4`
- [ ] 实现 `select_model()` 方法

**验证方式：**
- 不同规模的PR使用不同模型
- 安全相关变更使用最强模型

---

## 📊 阶段四：质量控制机制（预计 2-3 天）

### 功能点 4.1：Token预算管理
**目标：** 控制上下文Token使用，避免超出模型限制

**任务清单：**
- [ ] 定义各模型的上下文窗口限制
- [ ] 定义Token分配比例：
  - 系统提示词：2000
  - PR元数据：500
  - Diff内容：60%
  - 检索上下文：20%
  - 回复：15%
- [ ] 实现 `calculate_budget()` 方法
- [ ] 实现 `truncate_to_budget()` 方法（截断超长内容）

**验证方式：**
- Token使用不超过预算
- 超长内容被正确截断

---

### 功能点 4.2：置信度评估
**目标：** 为每个风险标记置信度等级

**任务清单：**
- [ ] 定义置信度等级（High/Medium/Low）
- [ ] 实现置信度计算逻辑：
  - 多个独立信号确认 → High
  - 单一信号但AI确认 → Medium
  - 不确定 → Low
- [ ] 在输出中标注置信度

**验证方式：**
- 每个风险都有置信度标注
- 用户可据此判断是否采纳

---

### 功能点 4.3：误报过滤
**目标：** 减少AI误报，提高评审质量

**任务清单：**
- [ ] 定义常见误报规则（如：测试代码中的mock数据）
- [ ] 实现上下文验证（检查风险是否在上下文中成立）
- [ ] 实现AI自我校验（让AI二次确认风险）
- [ ] 过滤低置信度风险

**验证方式：**
- 误报率降低
- 真实风险不被误过滤

---

### 功能点 4.4：多角度分析
**目标：** 从多个维度分析代码，减少漏报

**任务清单：**
- [ ] 实现安全角度分析
- [ ] 实现性能角度分析
- [ ] 实现稳定性角度分析
- [ ] 实现最佳实践角度分析
- [ ] 合并去重结果

**验证方式：**
- 漏报率降低
- 多维度风险都被识别

---

## 💻 阶段五：CLI界面开发（预计 2 天）

### 功能点 5.1：CLI框架搭建
**目标：** 使用Typer创建CLI命令框架

**任务清单：**
- [ ] 安装Typer和Rich库
- [ ] 创建主命令入口 `codesage`
- [ ] 定义子命令：`review`, `config`, `history`, `help`

**验证方式：**
- `codesage --help` 显示帮助信息
- 各子命令可正常调用

---

### 功能点 5.2：review命令实现
**目标：** 实现核心的PR评审命令

**任务清单：**
- [ ] 定义命令参数：
  - `pr_url`：PR地址（必填）
  - `--verbose`：详细输出
  - `--comment`：发布到PR评论
  - `--deep`：深度分析模式
- [ ] 实现命令流程：
  - 加载配置 → 获取PR → AI分析 → 输出结果
- [ ] 处理异常情况

**验证方式：**
- `codesage review <url>` 正常执行
- 输出格式清晰

---

### 功能点 5.3：config命令实现
**目标：** 实现配置管理命令

**任务清单：**
- [ ] 支持设置配置：`codesage config set <key> <value>`
- [ ] 支持获取配置：`codesage config get <key>`
- [ ] 支持列出配置：`codesage config list`

**验证方式：**
- 配置可以正常设置和读取

---

### 功能点 5.4：输出格式化
**目标：** 使用Rich库美化CLI输出

**任务清单：**
- [ ] 实现变更摘要输出（表格形式）
- [ ] 实现风险列表输出（表格 + 详细说明）
- [ ] 实现进度展示（进度条/Spinner）
- [ ] 实现颜色高亮（严重程度用不同颜色）
- [ ] 实现emoji图标（增强可读性）

**验证方式：**
- 输出美观清晰
- 信息层次分明

---

### 功能点 5.5：PR评论格式化
**目标：** 生成适合GitHub PR的Markdown评论

**任务清单：**
- [ ] 设计评论模板结构：
  - 变更摘要部分
  - 风险列表表格
  - 详细建议部分
- [ ] 生成Markdown格式内容
- [ ] 添加元信息（耗时、模型）

**验证方式：**
- 评论在GitHub中正确渲染
- 格式美观专业

---

## 🧪 阶段六：测试与优化（预计 2 天）

### 功能点 6.1：单元测试
**目标：** 为核心模块编写单元测试

**任务清单：**
- [ ] 测试PR URL解析
- [ ] 测试GitHub API封装
- [ ] 测试AI响应解析
- [ ] 测试模型路由逻辑
- [ ] 测试Token预算管理

**验证方式：**
- 所有测试通过
- 覆盖率 > 80%

---

### 功能点 6.2：集成测试
**目标：** 测试完整流程

**任务清单：**
- [ ] 测试完整的PR评审流程
- [ ] 测试配置管理流程
- [ ] 测试评论发布流程

**验证方式：**
- 端到端流程正常
- 各模块协作无误

---

### 功能点 6.3：性能优化
**目标：** 优化响应速度和资源消耗

**任务清单：**
- [ ] 实现缓存机制（缓存常见模式的分析结果）
- [ ] 优化并发处理（并行分析多个文件）
- [ ] 优化Token使用（精简提示词）

**验证方式：**
- 小型PR分析时间 < 10秒
- Token消耗降低

---

### 功能点 6.4：错误处理
**目标：** 完善错误处理和降级策略

**任务清单：**
- [ ] 处理GitHub API错误（限流、权限不足）
- [ ] 处理OpenAI API错误（超时、配额不足）
- [ ] 实现模型降级（主模型失败时切换备用模型）
- [ ] 提供清晰的错误提示

**验证方式：**
- 错误情况有明确提示
- 降级策略正常工作

---

## 📈 功能依赖关系图

```
阶段一（基础）
  │
  ├─ 1.1 项目初始化
  ├─ 1.2 数据模型 ←─────┐
  └─ 1.3 配置管理       │
                        │
阶段二（GitHub）        │
  │                     │
  ├─ 2.1 URL解析        │
  ├─ 2.2 PR元数据       │ 依赖 1.2
  ├─ 2.3 文件变更       │
  ├─ 2.4 Commit列表     │
  ├─ 2.5 完整获取       │ 依赖 2.1-2.4
  └─ 2.6 发布评论       │
                        │
阶段三（AI分析）        │
  │                     │
  ├─ 3.1 OpenAI封装     │ 依赖 1.3
  ├─ 3.2 提示词模板     │
  ├─ 3.3 变更摘要       │ 依赖 3.1, 3.2
  ├─ 3.4 风险检测       │ 依赖 3.1, 3.2
  ├─ 3.5 修复建议       │ 依赖 3.1, 3.2, 3.4
  ├─ 3.6 完整流程       │ 依赖 3.3-3.5
  └─ 3.7 模型路由       │
                        │
阶段四（质量控制）      │
  │                     │
  ├─ 4.1 Token管理      │ 依赖 3.1
  ├─ 4.2 置信度评估     │ 依赖 3.4
  ├─ 4.3 误报过滤       │ 依赖 3.4, 4.2
  └─ 4.4 多角度分析     │ 依赖 3.4
                        │
阶段五（CLI）           │
  │                     │
  ├─ 5.1 CLI框架        │
  ├─ 5.2 review命令     │ 依赖 2.5, 3.6
  ├─ 5.3 config命令     │ 依赖 1.3
  ├─ 5.4 输出格式化     │ 依赖 3.6
  └─ 5.5 评论格式化     │ 依赖 3.6, 2.6
                        │
阶段六（测试优化）      │
  │                     │
  ├─ 6.1 单元测试       │ 依赖 所有模块
  ├─ 6.2 集成测试       │ 依赖 5.2
  ├─ 6.3 性能优化       │ 依赖 3.6
  └─ 6.4 错误处理       │ 依赖 所有模块
```

---

## 🎯 建议实施顺序

### Week 1：基础 + GitHub集成
- Day 1-2：完成阶段一（项目基础）
- Day 3-5：完成阶段二（GitHub API）

### Week 2：AI分析引擎
- Day 1-2：完成功能点 3.1-3.3（OpenAI封装 + 摘要）
- Day 3-4：完成功能点 3.4-3.5（风险检测 + 建议）
- Day 5：完成功能点 3.6-3.7（完整流程 + 模型路由）

### Week 3：质量控制 + CLI
- Day 1-2：完成阶段四（质量控制）
- Day 3-4：完成阶段五（CLI界面）
- Day 5：完成阶段六（测试优化）

---

## 📝 每个功能点的验收标准

| 功能点 | 验收标准 |
|--------|----------|
| **1.1 项目初始化** | 项目结构完整，依赖安装成功 |
| **1.2 数据模型** | 所有模型可实例化，字段验证正常 |
| **1.3 配置管理** | 配置可保存/读取，环境变量生效 |
| **2.1 URL解析** | 有效URL正确解析，无效URL报错 |
| **2.2 PR元数据** | 公开PR可获取，私有PR需Token |
| **2.3 文件变更** | diff内容正确提取 |
| **2.4 Commit列表** | Commit信息完整 |
| **2.5 完整获取** | PR对象完整，统计信息正确 |
| **2.6 发布评论** | 评论成功发布 |
| **3.1 OpenAI封装** | API调用成功，响应正确 |
| **3.2 提示词模板** | 输出格式可解析 |
| **3.3 变更摘要** | 摘要准确反映意图 |
| **3.4 风险检测** | 识别常见风险 |
| **3.5 修复建议** | 建议具体可操作 |
| **3.6 完整流程** | 分析结果完整 |
| **3.7 模型路由** | 不同规模PR用不同模型 |
| **4.1 Token管理** | Token不超限 |
| **4.2 置信度评估** | 每个风险有置信度 |
| **4.3 误报过滤** | 误报率降低 |
| **4.4 多角度分析** | 漏报率降低 |
| **5.1 CLI框架** | 命令可正常调用 |
| **5.2 review命令** | 完整流程正常 |
| **5.3 config命令** | 配置可管理 |
| **5.4 输出格式化** | 输出美观清晰 |
| **5.5 评论格式化** | Markdown正确渲染 |
| **6.1 单元测试** | 测试通过，覆盖率>80% |
| **6.2 集成测试** | 端到端流程正常 |
| **6.3 性能优化** | 小PR<10秒 |
| **6.4 错误处理** | 错误提示清晰 |

---

## 🚀 快速启动建议

如果你想快速验证想法，建议按以下最小可行路径实施：

**MVP路径（最小可用版本）：**
1. 功能点 1.1 → 1.2 → 1.3（基础搭建）
2. 功能点 2.1 → 2.2 → 2.3 → 2.5（GitHub获取）
3. 功能点 3.1 → 3.2 → 3.3 → 3.4 → 3.6（AI分析）
4. 功能点 5.1 → 5.2 → 5.4（CLI输出）

**预计时间：5-7天**

完成MVP后，你就可以：
- 输入PR URL
- 获取AI分析结果
- 在CLI中查看评审报告

后续再逐步添加：评论发布、质量控制、深度分析等功能。

---

**下一步：** 请告诉我你想从哪个阶段开始，我会为你提供该阶段的详细实现指导。